<div class="container">
    <div class="col-12">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-info">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
    </div>
</div>
