{% extends 'base.html.twig' %}

{% block title %}Modifier une proposition{% endblock %}

{% block body %}
    <h2 class="m-4 font-weight-normal text-center">Modifier une proposition</h2>

    {{ include('proposition/_form.html.twig', {'button_label': 'Editer'}) }}

<div class="container">
    <div class="col-12">

        <a href="{{app.request.headers.get('referer') }}" class="btn btn-outline-info mt-2">Annuler</a>

        {{ include('proposition/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
