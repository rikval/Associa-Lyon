<form method="post" action="{{ path('proposition_delete', {'id': proposition.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette proposition?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposition.id) }}">
    <button class="btn btn-outline-info mt-1">Supprimer</button>
</form>
