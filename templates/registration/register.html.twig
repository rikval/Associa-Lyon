{% extends 'base.html.twig' %}

{% block title %}S'enregistrer{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12 col-md-6">
            <h2 class="m-4 font-weight-normal">Créer son espace personnel</h2><br>
            <p>Si les défis que notre société rencontre sont immenses, les solutions apportées par les citoyens progressent
            pour être à la hauteur. Et chacun d'entre nous peut y apporter son énergie, ses idées, ses compétences, sa motivation...</p>
        </div>
    <div class="col-12 col-md-6 mt-4">
    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.firstname) }}
        {{ form_row(registrationForm.surname) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword) }}

        <button class="btn btn-outline-info mt-4" style="float:right" type="submit">S'enregistrer</button>
    {{ form_end(registrationForm) }}
        </div>
    </div>
</div>
{% endblock %}
